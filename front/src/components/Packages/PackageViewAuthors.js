import React from "react";

const PackageViewAuthors = ({ package: { id, name, date, author: { id, name, postCount }, views, downloads} }) => { 
  const classes = useStyles();

  return (
    <>
    
    </>
  );
}

export default PackageViewAuthors;